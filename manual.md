# Gemini Video Analyzer 使い方マニュアル

このマニュアルは、Gemini Video Analyzer を初めて使う方や日常的に利用する方向けの操作ガイドです。画面録画やPC音声の録音から重要な場面をPC内で自動選別し、Gemini APIを活用して要約・文字起こし・画像解説付きの構造化PDFレポートを生成します。

> [!IMPORTANT]
> **プライバシーとデータ送信に関する基本方針**
> 元動画の全体をクラウドにアップロードすることはありません。PC内で抽出した音声データと、重要シーンとして選別された静止画候補のみを、ユーザーの確認を経てGemini APIへ送信します。

---

## 1. 初回準備（一度だけ実施）

### 動作要件
- **対応OS**: Windows 10 / 11
- **Python環境**: Python 3.10 以降（Tkinterを含む標準インストーラー版）
- **Gemini APIキー**: [Google AI Studio](https://aistudio.google.com/apikey) で作成（無料枠あり）

### インストール手順
1. GitHub Releases から `Gemini-Video-Analyzer-Windows-Japanese.zip` をダウンロードし、任意のフォルダに解凍します。
2. 解凍したフォルダ内で PowerShell を開き、以下のコマンドを実行して必要なライブラリをインストールします。

```powershell
py -3 -m pip install -r requirements-app.txt
```

> [!NOTE]
> `py` コマンドが見つからない場合は、Python 3.10以降のインストーラーを起動し、「Add Python to PATH」を有効にして再インストールしてください。

### APIキーの設定
1. [Google AI Studio](https://aistudio.google.com/apikey) で無料のAPIキーを作成します。
2. `Start.bat` の初回起動時に入力画面が表示されます。取得したキーを貼り付けて「OK」を押してください。
   ※「録音だけ保存（完全ローカル）」機能のみを利用する場合は、APIキーが未設定でも動作します。

---

## 2. 日常的な使い方

`Start.bat` をダブルクリックして起動します。ボタン操作のデスクトップ画面が開きます。

### 主な画面操作
画面上部には以下の操作ボタンが並んでいます。

- **「● 録画する」**: 画面とPC内部音声を録画し、終了後にAI解析とPDFレポートを生成します。
- **「🎙 録音する」**: 画面録画を行わず、PC内部音声のみをキャプチャします。
- **「動画を選ぶ」**: 保存済みの動画ファイルを選択してAI解析を行います。
- **「結果を見る」**: 直近の解析結果（PDFレポート）や保存先フォルダを開きます。
- **「設定」**: 表示言語、保存先フォルダ、画像候補数（既定: 30枚）や音声上限時間（既定: 120分）を変更できます。

---

### 操作手順1: 画面と音声を録画して解析する

1. **「● 録画する」をクリックします。**
2. 録画範囲の選択画面が表示されます。「すべての画面」「モニターを1台選ぶ」「マウスで範囲指定」から録画エリアを選びます。
3. **PC音声のテストが始まります。** PCで動画や音声を再生した状態で3秒間お待ちください。音声が正常に検出されると録画が開始されます。
4. 録画中は、画面上の「一時停止／再開」ボタンで一時停止が可能です（一時停止中の内容は記録されません）。
5. 録画を終えるときは「録画を終了して解析」をクリックします。
6. PC内で音声抽出と重要シーンの選別が行われた後、クラウド送信の確認ダイアログが表示されます。内容を確認して「同意して続行」を押します。
7. AI解析とPDFレポートの生成が自動で進みます。完了すると「結果を見る」からPDFを開けます。

> [!WARNING]
> 録画範囲内に通知ポップアップや個人チャットなどの機密情報が映り込まないよう、録画開始前にご確認ください。また、会議や講義を録画する際は参加者の同意を得た上でご利用ください。

---

### 操作手順2: PC音声のみを録音する

1. **「🎙 録音する」をクリックします。**
2. 動作モードの選択画面が表示されます。
   - **「録音だけ保存」**: 完全ローカルで動作します。APIを使用せず、APIキーが未設定でも利用できます。高音質な音声ファイルが保存されます。
   - **「録音して文字起こし」**: 録音終了後、Gemini APIを利用して文字起こしと要約PDFレポートを生成します。
3. 録音を終えるときは「録画を終了して解析」（録音停止）をクリックします。

---

### 操作手順3: 保存済みの動画を解析する

1. **「動画を選ぶ」をクリックして動画ファイルを選択します。**
   ※動画ファイルを `Start.bat` のアイコンへ直接ドラッグ＆ドロップすることでも解析を開始できます。
2. 送信確認画面で内容を確認し、解析を開始します。


---

## 3. 送信されるデータとプライバシー

- **送信対象**: PC内で選別した場面候補画像（静止画）と、文字起こし用の音声のみです。元動画全体をクラウドへ送信することはありません。
- **使用モデル**: 高速・高精度な `gemini-3.5-flash` を標準で使用します。混雑時や利用制限（429エラー等）の発生時は、`gemini-2.5-flash` や `gemini-3.1-flash-lite` へ自動で切り替わります。
- **自動再試行**: 一時的な通信エラーやAPI制限に対しては、待機時間を段階的に延ばしながら自動で再試行します。
- **クリーンアップ**: 正常にPDFが生成された後、途中で抽出した大量の中間画像や作業ファイルは自動で削除されます。

---

## 4. 出力されるファイルと保存先

録画セッションが完了すると、成果物は `output/local_recordings/録画_<日時>/` に整理されて保存されます。
（保存済み動画を解析した場合は、元動画と同じフォルダ内に `動画名_解析結果_<日時>/` が作られます）

| ファイル名 | 内容 |
|---|---|
| `画面録画.mp4` | 録画した元動画（画面録画時のみ） |
| `PC音声.wav`（または圧縮音声） | 録音されたクリアなPC内部音声 |
| `AI解析結果/<タイトル>_解析レポート.pdf` | 各場面の時刻・重要度・解説・検出テキストと、文字起こし全文をまとめた構造化PDF |

> [!NOTE]
> PDFレポート内に大容量の画像そのものは埋め込まれません。要約、重要シーンの解説、検出テキスト、および文字起こし全文が読みやすくレイアウトされます。

---

## 5. 中断と復旧（トラブルシューティング）

### 処理の安全な中断
解析中に「処理を中断（保存）」ボタンを押すと、実行中の通信やファイル書き込みを安全に完了させてから中断します。

### 途中からの再開
通信エラーやアプリの終了などで処理が中断した場合でも、完了済みの文字起こし区間や画像解析データは自動保存されています。
1. `Start.bat` を起動します。
2. 画面下部の「未完了の処理・録画の復旧」一覧から、再開したい項目を選択します。
3. 「選択した処理を再開」をクリックします（または項目をダブルクリック）。
4. 既に完了した処理を再利用し、中断された箇所から効率的に続行されます。

### 録画クラッシュ時の復旧
録画中は約60秒ごとに分割ファイルがディスクに書き出されています。万が一PCの電源断や異常終了が発生した場合は、「未完了の処理・録画の復旧」一覧から対象を選んで「録画を復旧」を実行することで、確定済みの区間を復元できます。

---

## 6. よくある質問・困ったとき

### `No module named ...` とエラーが出る
初回準備のライブラリインストールが完了していません。解凍フォルダで以下を実行してください。
```powershell
py -3 -m pip install -r requirements-app.txt
```

### PC音声が録音されない・無音になる
- 録画開始前の3秒間音声テスト中は、ブラウザやメディアプレーヤーで実際に音声を再生してください。
- Windowsの「サウンドの設定」を開き、再生デバイス（スピーカーやヘッドホン）が正しく選択されているか確認してください。
- Bluetooth機器の接続直後は認識が不安定になる場合があります。アプリを一度閉じて再起動してください。

### FFmpegを導入して音声圧縮を行いたい
本アプリはFFmpegがインストールされている場合、Opus（32 kbps）およびM4A（48 kbps）による最大約98%の超軽量圧縮を自動適用します（未導入でも16 kHz WAVで問題なく動作します）。
PowerShellで以下のコマンドを実行することで簡単に導入できます。
```powershell
winget install Gyan.FFmpeg
```

### 従来のコマンドライン画面（CUI）を使いたい
旧来の番号選択式コンソール画面を利用したい場合は、PowerShellから以下を実行してください。
```powershell
python launcher.py
```

---

## 7. フォルダを他者に配布・共有するときの注意

本フォルダをZIP化して他のPCや第三者へ渡す場合は、事前に以下の点をご確認ください。
1. `output` フォルダ内の録画・音声・解析結果ファイルは削除してください。
2. `.env` ファイルが存在する場合は、APIキーが含まれているため必ず削除してください。
3. 利用者ごとに各自のGoogle AI StudioでGemini APIキーを取得して入力してもらってください。
