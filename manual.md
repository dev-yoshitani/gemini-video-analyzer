# Gemini Video Analyzer 使い方マニュアル

このマニュアルは、現在の配布版を使うための案内です。録画または保存済み動画から、PC内で変化の大きい場面候補を選び、Geminiで文字起こしと場面の説明を作成します。

> [!IMPORTANT]
> すべてをPC内で完結する方式ではありません。元動画はPC内に残りますが、確認後に音声と選別済みの場面候補画像をGemini APIへ送信します。

---

## 1. 最初に一度だけ行う準備

必要なものは、Windows 10/11、Python 3.10以降、Gemini APIキーです。APIの利用枠・料金は、ご利用のGoogleアカウントとプランに従います。

1. GitHub ReleasesからZIPをダウンロードし、任意のフォルダへ解凍します。
2. 解凍したフォルダでPowerShellを開き、次を実行します。

```powershell
py -3 -m pip install -r requirements-app.txt
```

3. [Google AI Studio](https://aistudio.google.com/apikey)でGemini APIキーを作成します。
4. `Start.bat`を初めて起動したとき、画面の案内に従ってAPIキーを入力します。キーは他の人と共有しないでください。

> [!NOTE]
> `py`が見つからない場合は、Python 3.10以降をインストールし、インストーラーの「Add Python to PATH」を有効にしてから、もう一度実行してください。

---

## 2. ふだんの使い方

`Start.bat`をダブルクリックします。表示されたメニューから、目的に合う番号を選びます。

| 番号 | できること |
|---|---|
| 1 | 画面とPC音声を録画し、高精度AI解析とPDF作成を行う |
| 2 | 保存済み動画を高精度AI解析し、PDFを作成する |
| 3 | 通信エラーや利用制限で止まった解析を途中から再開する |
| 4 | 従来の音声のみ文字起こしを使う |

動画ファイルを`Start.bat`へドラッグ＆ドロップしても、保存済み動画の高精度AI解析を開始できます。

### 録画して解析する

1. メニューで`1`を選びます。
2. 「すべての画面」「モニターを1台選ぶ」「マウスで範囲指定」から録画範囲を選びます。
3. PCで動画や音楽を再生した状態にしてEnterキーを押します。3秒間のPC音声テストに合格した場合だけ録画が始まります。
4. 録画を止めるときに、もう一度Enterキーを押します。
5. PC内でシーン候補と音声を準備した後、Geminiへ送る前の確認が表示されます。内容を確認して続行します。

通知、個人チャット、パスワードなどが録画範囲に映らないよう、開始前に確認してください。会議を録画する場合は、必要な許可も得てください。

### 保存済み動画を解析する

1. メニューで`2`を選ぶか、動画を`Start.bat`へドラッグ＆ドロップします。
2. 動画ファイルを選び、Geminiへの送信内容を確認します。
3. 処理が終わるまで待ちます。途中で止まった場合は、次回メニューの`3`から再開できます。

---

## 3. 送信されるデータとプライバシー

- 元動画全体はGeminiへ送信しません。
- PC内で選別した場面候補画像と、文字起こし用の音声をGeminiへ送信します。
- Gemini 3.5 Flashを標準で使い、利用制限・混雑時はGemini 2.5 Flash、次にGemini 3.1 Flash-Liteへ自動で切り替えます。
- 一時的なAPI制限や混雑では15秒、30秒、60秒、120秒と段階的に待機して自動再試行します。
- 正常終了後は録画、PC音声、PDFだけを残し、中間ファイルは自動削除します。

---

## 4. 出力されるファイル

録画した場合は、`output/local_recordings/録画_日時/`に保存されます。保存済み動画を解析した場合は、元動画と同じフォルダに`動画名_解析結果_日時/`が作られます。

主な出力は次のとおりです。

| ファイル・フォルダ | 内容 |
|---|---|
| `画面録画.mp4`または`画面録画.avi` | 録画した元動画（録画した場合のみ） |
| `PC音声.wav` | 録音したPC音声（録画した場合のみ） |
| `内容のタイトル_解析レポート.pdf` | 場面の時刻・重要度・説明・検出テキストと、文字起こしをまとめたPDF |

PDFには場面画像そのものを埋め込みません。画像の時刻、重要度、解析結果、画像内の主要テキストを読みやすくまとめます。抽出画像、文字起こしTXT、Markdown、JSON、解析用音声、再開データは、PDFが正常に完成した後で自動削除されます。

---

## 5. 途中で止まったとき

一時的なAPI利用制限や混雑では、アプリが待機して自動再試行します。それでも完了できない場合、通信エラー、アプリ終了などで処理が止まった場合は、完了済みの候補抽出・文字起こし・画像解析が一時保存されます。

1. `Start.bat`を開きます。
2. メニューで`3`を選びます。
3. 再開したい動画を選びます。

---

## 6. 困ったとき

### `No module named ...`と表示される

初回準備のコマンドを、解凍したフォルダで実行してください。

```powershell
py -3 -m pip install -r requirements-app.txt
```

### APIキーが設定されていない

`Start.bat`を起動し、表示された入力欄にご自身のGemini APIキーを貼り付けます。キーを公開した場合はGoogle AI Studioで失効し、新しいキーを作成してください。

### 解析が途中で止まった

通信状況とGeminiの利用枠を確認し、メニューの`3`から再開してください。自動切り替え後のモデルでも利用制限が続く場合は、時間をおいて再試行してください。

### PC音声を録音できない

録画前の3秒間テスト中は、PCで動画や音楽を実際に再生してください。Windowsの「サウンド設定」で、再生先のスピーカーやヘッドホンが正しく選ばれているか確認します。Bluetooth機器を切り替えた直後は、一度アプリを終了してから起動し直してください。テスト後に本録音だけを開始できなかった場合は、画面録画も自動的に中止されます。

---

## 7. 他の人に渡すとき

フォルダをZIPにして共有できます。ただし、共有前に`output`フォルダ内の録画・音声・解析結果を削除し、`.env`ファイルがあれば必ず除外してください。APIキーは各利用者が自分で作成し、入力してください。
